function src_file {
    if [ -f "$1" ]; then
        source $1
    fi
}

src_file /usr/share/zsh/site-functions/aws_zsh_completer.sh
src_file "$HOME/.homesick/repos/homeshick/homeshick.sh"
src_file "$HOME/.alias"
src_file "$HOME/.localalias"
src_file /etc/bash_completion.d/docker-machine
